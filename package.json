{
  "name": "acf-blokken-test",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./style.css",
    "dev": "concurrently \"tailwindcss -i ./src/input.css -o ./style.css --watch\" \"browser-sync start --proxy 'acf-blokken-test.local' --files '**/*.php, **/*.css'\""
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.2.1",
    "tailwindcss": "^3.4.0"
  }
}
